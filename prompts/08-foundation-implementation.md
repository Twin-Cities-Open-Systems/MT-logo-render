# Agent Prompt 08 — Foundation Implementation

## Objective
Implement the core Rust project structure and basic CLI framework:
- Cargo.toml with minimal dependencies
- Basic CLI argument parsing with clap
- Project module structure and error handling
- Basic command stubs for resolve/render/doctor/list

## Output
- Create src/ directory with initial module structure
- Implement basic CLI framework with command routing
- Set up error handling with thiserror/anyhow
- Document dependency choices and security considerations

## Constraints
- Implement only foundation, no rendering logic yet.
- Focus on compile-time safety and clear error messages.
- Design for testability from day one.
